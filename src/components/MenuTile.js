import React from "react";

function MenuTile({
    backgroundImageURL, 
    topMargin,
    height,
    width,
    backgroundBasicColor, 
    backgroundHoverColor,
    svgBasicColor,
    svgHoverColor,
    darkModeOn,

}) {

    return (
  <div></div>
      );
}

export default MenuTile;
